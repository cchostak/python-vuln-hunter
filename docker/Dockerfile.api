FROM python:3.10-slim
WORKDIR /workspace
COPY . .
RUN pip install --no-cache-dir -r requirements.txt
EXPOSE 8000
CMD ["uvicorn", "vuln_hunter.inference.api:app", "--host", "0.0.0.0", "--port", "8000"]
